public abstract class WindInstrument extends Instrument { //Abstract class that extends Instrument
   //The playFSharp method inherithed is still abstract (no override) because too generic!
   // . . .
}